packages:

# tests and macros
  - package: dbt-labs/dbt_utils
    version: 1.0.0

# tests
  - package: calogica/dbt_expectations
    version: [">=0.8.0", "<0.9.0"]

# usecases: generate a yml file template for a model
# or generate a model SQL for a source table in BQ/...
  - package: dbt-labs/codegen
    version: 0.9.0

# run dbt deps to install the packages from this file